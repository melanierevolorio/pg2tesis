@page
@model             SuperGasSecurity.Pages.Account.Login.Index

<div class="login-page">
    <div class="lead" >
        <h1 class="text-center">Super Gas </h1>.
    </div>

    <partial name="_ValidationSummary" />

    <div class="row justify-content-md-center">

        @if (Model.View.EnableLocalLogin)
        {
            <div class="col-xs- 6 col-sm-4">
                <div class="card">
                    <div class="card-header">

                        <h4 class="text-center table-active">Inicio de sesión</h4>
                    </div>
                    <div class="card-body">
                        <form asp-page="/Account/Login/Index">
                            <input type="hidden" asp-for="Input.ReturnUrl" />

                            <div class="form-group">
                                <label asp-for="Input.Username"></label>
                                <input class="form-control" placeholder="Usuario" asp-for="Input.Username" autofocus>
                            </div>
                            <div class="form-group">
                                <label asp-for="Input.Password"></label>
                                <input type="password" class="form-control" placeholder="Contraseña" asp-for="Input.Password" autocomplete="off">
                            </div>
                         
                            @if (Model.View.AllowRememberLogin)
                            {
                                <div class="form-group">
                                    <div class="form-check form-switch">
                                        <input class="form-check-input" type="checkbox" id="flexSwitchCheckChecked"  asp-for="Input.RememberLogin">
                                        <label class="form-check-label" for="flexSwitchCheckDefault" asp-for="Input.RememberLogin">
                                            Recordarme
                                        </label>
                                   
                                    </div>
                                </div>
                            }
                            <div class="col text-center">
                                <button class="btn btn-primary" name="Input.Button" value="login">Ingresar</button>
                            </div>
                            
                         @*\   <button class="btn btn-secondary" name="Input.Button" value="cancel">Cancelar</button>*@
                        </form>
                    </div>
                </div>
            </div>
           
        }
    </div>
</div>